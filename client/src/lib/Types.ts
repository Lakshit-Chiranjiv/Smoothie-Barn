export type userStateType = {
    email: string,
    username: string,
    token: string
}

type SmoothieProps = {
    createdAt: string,
    createdBy: string,
    name: string,
    price: number,
    steps: string[],
    updatedAt: string,
    _v: number,
    _id: string
}